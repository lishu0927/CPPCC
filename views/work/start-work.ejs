<!DOCTYPE html>
<html>
<%-include ../base/checkHead.ejs%>
<style>
    .container{
        padding: 82px 0 0 0;
    }
    .header-right{
        position: absolute;
        z-index: 10000000;
        height: 42px;
        text-align: center;
        line-height: 42px;
        top:0;
        right: 0;
        padding: 0 15px 0 10px;
        color: #fff;
        font-size: 14px;
    }
    .states-content{
        position: fixed;
        top:42px;
        width: 100%;
        height: 40px;
        z-index: 100000;
    }
    .list-tab{
        display: flex;
        border:1px solid #0071bd;
        height: 40px;
        padding: 5px 0;
    }
    .list-tab .tab-item{
        flex: 1;
        height: 100%;
        float: left;
        line-height: 30px;
        text-align: center;
        color: #585757;
        font-size: 14px;
        border-right: 1px solid #0071bd;
        cursor: pointer;
    }
    .list-tab .tab-item:last-child{
        border-right: 0;
    }
    .list-tab .tab-item.active{
        color: #0071bd;
    }
    .item{
        padding: 10px;
        border-bottom: 1px solid #bcbcbc;
        position: relative;
        display: block;
    }
    .item .circle{
        position: absolute;
        top:17px;
        left: 13px;
        background: #0071bd;
        width: 10px;
        height: 10px;
    }
    .item .anyou{
        line-height: 22px;
        color: #3b3b3b;
        text-indent: 20px;
        font-size: 13px;
        min-height: 22px;
    }
    .item .bottom{
        font-size: 12px;
        color: #0071bd;
        text-align: right;
        overflow-y: auto;
        padding-top: 3px;
    }
    .item .bottom .left{
        float: left;
        padding: 0 10px 0 0;
    }
    .item .bottom .right{
        float: right;
    }
</style>
<body>
<div class="container">
    <%-include ../base/header.ejs%>
    <a class="header-right" href="/addWork">添加</a>
    <div class="wrap">
        <div class="states-content">
            <div class="list-tab">
                <span class="tab-item active" id="item1">草稿</span>
                <span class="tab-item" id="item2">提交文件</span>
            </div>
        </div>
        <div class="list-content" id="list-content">
        </div>
    </div>
</div>
</body>
<script>
    var dailywork_list_kind;
    var detail_type;
    var user_id=sessionStorage.getItem("user_id")
    $(".tab-item").each(function (i) {
        $(this).click(function () {
            window.history.replaceState({},0,window.location.href.split("?")[0]+"?tabIndex="+i)
            $(this).addClass("active");
            $(this).siblings().attr("class","tab-item");
            var itemId=$(this).attr("id");
            if(itemId=='item1'){
                dailywork_list_kind=1
                detail_type=1
            }else if(itemId=='item2'){
                dailywork_list_kind=2
                detail_type=2
            }else{
                return ;
            }
            var params = {
                dailywork_list_kind:dailywork_list_kind,
                user_id:user_id
            }
            var data = JSON.stringify(params)
            $.ajax({
                url: 'http://114.215.98.96:8011/ZxApi/m3_05.ashx',
                type: "POST",
                dataType: 'json',
                data: data,
                error: function (request) {      // 设置表单提交出错
                    mui.toast("系统错误，返回状态码为：" + request.status);
                },
                success: function (data) {
                    if(data.ecode==1000){
                        $("#list-content").html("");
                        var list=data.dailywork_list
                        console.log(list)
                        $.each(list, function (i) {
                            var item='<a class="item" href="/workDetail'+detail_type+'?id='+list[i].dailywork_id+'">' +
                                '<span class="circle"></span>' +
                                '<p class="anyou">'+list[i].worktitle+'</p>' +
                                '<p class="bottom">' +
                                '<span class="left">'+list[i].worknumber+'</span>' +
                                '<span class="left">'+list[i].username+'</span>' +
                                '<span class="left">'+ list[i].createdate.split(" ")[0] +'</span>' +
                                '</p>' +
                                '</a>'
                            $("#list-content").append(item);
                        })
                    }
                }
            });
        })
    })
    $(function () {
        var tabIndex="<%=tabIndex%>"
        console.log(tabIndex)
        $(".tab-item").eq(tabIndex).click();
    })
</script>
</html>
