<!DOCTYPE html>
<html>
<%-include ../base/checkHead.ejs%>
<body>
<div class="container">
    <%-include ../base/header.ejs%>
    <div class="wrap">
        <div class="row">
            <input type="text" placeholder="请输入原始密码" id="pwd"/>
        </div>
        <div class="row">
            <input type="text" placeholder="请输入新密码" id="newpwd"/>
        </div>
        <div class="row">
            <input type="text" placeholder="请再次输入新密码" id="reppwd"/>
        </div>
        <div class="confirm">
            <span>确认修改</span>
        </div>
    </div>
</div>
</body>
<style>
    .container{
        padding: 42px 0 0 0;
    }
    .wrap{
        padding: 15px;
    }
    .wrap .row{
    }
    .wrap .row input{
        width: 100%;
        border: 1px solid #00a0e9;
        text-indent: 10px;
    }
    .confirm{
        text-align: center;
    }
    .confirm span{
        background: #e1edfd;
        border-radius: 5px;
        padding: 12px ;
        display: block;
        color: #343232;
        margin-top: 30px;
        font-size: 16px;
    }
</style>
</html>
<script>
    var user=JSON.parse(sessionStorage.getItem('user'))
    $('.confirm').click(function () {
        var psw = $('#pwd').val();
        var newpsw = $('#newpwd').val();
        var reppwd = $('#reppwd').val();
        if(newpsw==""){
            mui.toast('新密码不能为空');
            return;
        }else if(newpsw!=reppwd){
            mui.toast('新密码两次输入不相同');
            return;
        }
        var params = {
            manager_id: user.manager_id,
            old_password: psw,
            new_password: newpsw
        }
        var data = JSON.stringify(params)
        $.ajax({
            url: 'http://114.215.98.96:8011/ZxApi/m4_01.ashx',
            type: "POST",
            dataType: 'json',
            data: data,
            error: function (request) {      // 设置表单提交出错
                mui.toast("系统错误，返回状态码为：" + request.status);
            },
            success: function (data) {
                if(data.ecode==1000){
                    mui.toast('修改成功');
                    setTimeout(function () {
                        window.history.back();
                    },1000)
                }else if(data.ecode==3103){
                    mui.toast('旧密码错误');
                }else{
                    mui.toast('修改失败');
                }
            }
        });
    });
</script>
